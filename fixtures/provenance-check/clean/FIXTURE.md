> Fixture: targets Dictum v1.2.0. Defect: clean

A fully synthetic geometry helper (invented product, MIT-outbound). One
non-trivial unit ports code from an invented upstream and attests it with a
`SOURCE:` marker declaring a **compatible** license (`BSD-3-Clause` into an
MIT product). Expected: **0 errors, 0 warnings** — the declaration is present
and its license is permissive-compatible with the MIT outbound.
