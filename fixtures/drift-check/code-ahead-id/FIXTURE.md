> Fixture: targets Dictum v1.1.0. Defect: code-ahead-id

As `clean`, plus `src/export.go` annotating `LIB-EXPORT` — a contract ID the
doc set's ID web does not own. Expected: exactly one certain-tier
`code-ahead` ERROR (code realizes a contract no doc owns), decidable under
the DICT-annotation + register-form conventions.
