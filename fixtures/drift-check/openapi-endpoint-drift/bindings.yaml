bindings:
  API-LIST-NOTES:
    locators:
      - { path: src/server.go, symbol: handleList }
  API-CREATE-NOTE:
    locators:
      - { path: src/server.go, symbol: handleCreate }
  ENTITY-NOTE:
    compare_via: openapi
    locators:
      - { path: src/note.py, symbol: Note }
