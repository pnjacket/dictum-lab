> Fixture: targets Dictum v1.1.0. Defect: clean

A fully synthetic note-taking service, built and bound: two API contracts and
an entity owned in the docs, a binding map whose locators all resolve, in-code
`DICT:` annotations matching the web, and an OpenAPI artifact matching the
register-line routes. `dictum-drift-check` must report 0 errors (the only
expected output is the certain-tier decline for field-level entity
comparison, which is not decidable from prose).
