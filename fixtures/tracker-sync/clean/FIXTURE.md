> Fixture: targets Dictum v1.1.0. Defect: clean

A fully synthetic note-taking service whose file-backed mock tracker exactly
mirrors the repo projection: one execution item (slice 1, Verified at merge,
hence closed) carrying the contract IDs as labels, plus one DEMAND item the
tool must never touch (Part 10c three item roles). Expected: 0 actions,
exit 0.
